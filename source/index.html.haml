.row
  .col-md-2.hidden-xs.hidden-sm.fixed
    %h3 Areas
    %ul.nav.nav-stacked{style: "background-color: #FAFAFA"}
      - SurveyResult.cached_data(data).each do |result|
        %li= link_to result.area.titleize, "##{snakecase(result.area)}"

  .col-md-10.col-md-offset-3.content
    %h1 Online Gaming Latency Report (Philippines)
    .lead
      %p
        Average latency/ping in Online Games reported by gamers in the Philippines.
      %p
        Contribute to the cause #{link_to "Fill up this survey", "https://docs.google.com/forms/d/1c-lECY5cFUiAbGuuuECvE5IXFBHA5L5IrSdxSaieLqM/viewform", class: 'btn btn-primary'}
    %hr
    - SurveyResult.cached_data(data).each do |result|
      = partial '_result', locals: { result: result }
      %hr
